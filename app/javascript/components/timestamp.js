import React from 'react'

const Timestamp = (props) => {
  return (
    <h1>**TODO-- Fill out timestamp component somehow...</h1>
  )
}

export default Timestamp
//<em><strong><%= link_to post.profile.name, post.profile, class: 'timestamp-username-link'%></strong> <%= post.created_at.strftime("%Y-%m-%d") %> ( <%= time_ago_in_words(post.created_at) %> ago)</em>
